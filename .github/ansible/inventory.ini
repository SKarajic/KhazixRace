# Local / manual deployment inventory — fill in your server details.
# GitHub Actions passes the host dynamically via "-i your-server-ip,"
# so this file is only needed for running deploys from your own machine.

[production]
ssh.khazixmains.com ansible_user=root ansible_ssh_private_key_file=~/.ssh/id_ed25519

[production:vars]
github_repository=skarajic/khazixrace
image_tag=latest
ghcr_username=skarajic
